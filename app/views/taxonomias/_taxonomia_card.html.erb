<div class="bg-white shadow-md rounded-lg p-4">
    <div class="flex justify-between items-center mb-2">
        <h3 class="text-lg font-semibold">
            <%= link_to taxonomia.gallo.placa, gallo_path(taxonomia.gallo) if taxonomia.gallo %>
        </h3>
    </div>
    
    <div class="space-y-2">
        <div>
            <span class="text-gray-600 font-medium">Padre: </span>
            <%= link_to taxonomia.padre.placa, gallo_path(taxonomia.padre) if taxonomia.padre %>
        </div>
        <div>
            <span class="text-gray-600 font-medium">Madre: </span>
            <%= link_to taxonomia.madre.placa, gallo_path(taxonomia.madre) if taxonomia.madre %>
        </div>
    </div>

    <div class="mt-4 flex justify-between">
        <%= link_to "Ver", taxonomia_path(taxonomia), 
            class: "text-blue-600 hover:text-blue-900" %>
        <%= link_to "Editar", edit_taxonomia_path(taxonomia), 
            class: "text-green-600 hover:text-green-900" %>
        <%= link_to "Eliminar", taxonomia_path(taxonomia), 
            method: :delete, 
            data: { confirm: "¿Estás seguro de eliminar esta taxonomía?" }, 
            class: "text-red-600 hover:text-red-900" %>
    </div>
</div>
